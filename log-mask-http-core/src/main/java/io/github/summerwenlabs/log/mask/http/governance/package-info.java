/**
 * Provides Spring-free URI, path, query, and header governance.
 *
 * <p>Use the rule and governance types to apply only explicitly configured
 * actions. {@link HttpRequestUri} exposes the resulting immutable URI log
 * representation while preserving the original request URI.
 *
 * @since 0.1
 */
package io.github.summerwenlabs.log.mask.http.governance;
